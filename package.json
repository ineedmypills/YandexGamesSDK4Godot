{
  "name": "yandex-integration-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:integration": "npx playwright test tests/yandex.spec.js --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
